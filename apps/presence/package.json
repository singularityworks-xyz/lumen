{
  "name": "@lumen/presence",
  "version": "1.0.39",
  "private": true,
  "scripts": {
    "dev": "mix phx.server",
    "build": "mix compile",
    "check": "mix format && mix compile --warnings-as-errors && mix credo --strict",
    "deps": "mix deps.get"
  }
}
