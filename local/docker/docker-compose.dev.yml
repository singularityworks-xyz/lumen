services:
  lumendb:
    image: postgres:18.3
    container_name: lumendb
    restart: unless-stopped
    environment:
      POSTGRES_DB: lumendb
      POSTGRES_USER: lumen
      POSTGRES_PASSWORD: lumen
    ports:
      - "5154:5432"
    volumes:
      - lumendb-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lumen -d lumendb"]
      interval: 5s
      timeout: 5s
      retries: 20

  lumencache:
    image: redis:8.6.2
    container_name: lumencache
    restart: unless-stopped
    ports:
      - "5354:6379"
    volumes:
      - lumencache-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20

  openobserve:
    image: public.ecr.aws/zinclabs/openobserve:latest
    container_name: lumen-openobserve
    environment:
      ZO_DATA_DIR: /data
      ZO_ROOT_USER_EMAIL: root@lumen.dev
      ZO_ROOT_USER_PASSWORD: lumen
      ZO_HTTP_PORT: "5080"
    ports:
      - "5080:5080"
      - "5081:5081"
    volumes:
      - openobserve-data:/data

volumes:
  lumendb-data:
  lumencache-data:
  openobserve-data:
